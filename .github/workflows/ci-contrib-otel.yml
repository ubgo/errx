name: CI contrib/otel

on:
  push:
    branches: [main]
    paths: ["contrib/otel/**", "*.go", "go.mod"]
  pull_request:
    paths: ["contrib/otel/**", "*.go", "go.mod"]

permissions:
  contents: read

defaults:
  run:
    working-directory: contrib/otel

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25" # contrib/otel tracks OpenTelemetry v1.43+ which requires Go 1.25
      - run: |
          out=$(gofmt -l -s .)
          if [ -n "$out" ]; then echo "$out"; exit 1; fi
      - run: go vet ./...
      - run: go test ./... -race -count=1
